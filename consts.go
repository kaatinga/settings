package env_loader

const (
	// env — env tag name
	env = "env"

	// duration — type time.Duration
	duration = "time.Duration"

	// logrusLevel — type logrus.Level
	logrusLevel = "logrus.Level"

	// syslogPriority — type syslog.Priority
	syslogPriority = "syslog.Priority"

	// required — the string that indicates that the field is required
	required = "required"

	//zerologLevel — type zerolog.Level
	zerologLevel = "zerolog.Level"
)
