package env_loader

const (
	// env — env tag name
	env = "env"

	// duration — type time.Duration
	duration = "time.Duration"

	// logLevel — type logrus.Level
	logLevel = "logrus.Level"

	// syslogPriority — type syslog.Priority
	syslogPriority = "syslog.Priority"
)
